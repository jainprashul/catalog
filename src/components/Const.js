const APPLINKS = {
    catalog: '/catalog',
    item: (id) => (`/item/${id}`),
}

const APPSTRING = {
    catalogItems: 'catalogItems',
    shops: 'shops',
}


export default APPLINKS
export  {APPSTRING};
